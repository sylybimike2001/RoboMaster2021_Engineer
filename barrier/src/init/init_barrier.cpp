#include <barrier.h>
#include <uvc_v4l2.h>
bool Barrier::initBarrier() {
    //使用
//    cutecap  = VideoCapture(capture_index);
//    if(cutecap.isOpened()) std::cout<<"open camera"<<std::endl;
//    //rmSerial.init();
//    cutecap.set(CAP_PROP_EXPOSURE,0.25);

}

#include <barrier.h>

void Barrier::getFitPoints(vector<Rect> &all_fit_rects, vector<Point> &all_fit_points) {
    Point center;
    for(auto item : all_fit_rects){
        //center.x = item.
    }
}
